# Eve

Treat the agent wallet as a workspace-scoped financial capability. Read only the bound wallet and submit writes as policy-checked intents. Never choose a tenant, source wallet, approval override, or credential from model input.
