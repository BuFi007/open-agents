## Gateman Verification Report

**Feature:** BU-228 durable ERP effect commands
**Branch / PR:** codex/bu-228-exactly-once-effects
**Date:** 2026-07-10
**Verifier:** Codex

### Score

| Category | Score | Note |
| --- | ---: | --- |
| Error handling | 8 | Invalid IDs fail loud and ambiguous commands cannot be claimed again before reconciliation. |
| Logging | 6 | Pure store contract emits no logs; workflow callers must append Eve trace events per command transition. |
| Type safety | 9 | Command kinds, statuses, providers, capabilities, and match factors are explicit unions. |
| Testability | 9 | Idempotent payable creation, provider alias normalization, ambiguous retry blocking, and match evidence are tested. |
| Performance | 8 | In-memory contract is O(1) by command and idempotency key; production store must preserve unique constraints. |
| Security | 8 | Provider tenant is part of the command key; no credentials, raw payloads, or provider secrets are stored. |
| AI verification | 8 | Edited files were re-read, imports verified by typecheck, and focused tests were observed passing. |

### Checks Passed

- [x] `CreatePayableFromArtifact` is keyed by canonical SourceArtifact.
- [x] `ExportPayableToERP` is keyed by workspace, bill, normalized provider, provider tenant, and command version.
- [x] Repeated upsert returns canonical command state rather than creating a duplicate effect.
- [x] Ambiguous provider outcomes block retry claims until reconciliation records a confirmed or failed result.
- [x] Provider alias drift is normalized for `conta-azul` -> `contaazul`, plus QuickBooks/QBO and Contabilium.
- [x] Provider capabilities capture idempotency, lookup, search window, void/update, and attachment support.
- [x] Match evidence includes threshold, score, source/target values, factor hashes, override, and decision.
- [x] Focused tests passed: `bun test packages/effects packages/connectors` -> 10 pass, 0 fail.
- [x] Typecheck passed: `bun run --cwd packages/effects typecheck`.
- [x] Whitespace check passed: `git diff --check`.

### Checks Failed

- [ ] No real provider sandbox calls are executed in this package.
- [ ] No Postgres transaction, unique index, or BullMQ worker claim implementation exists here.
- [ ] Eve trace emission is not wired to command transitions in this slice.

### Recommended Next Steps

1. Back this contract with Postgres command/outbox tables and unique constraints.
2. Add sandbox provider adapters that reconcile ambiguous outcomes before repeat creates.
3. Attach artifact -> payable -> approval -> export -> reconciliation trace events in the workflow layer.

### Risk Level

MEDIUM

### Sign-off

Safe to ship: YES_WITH_FOLLOWUPS
