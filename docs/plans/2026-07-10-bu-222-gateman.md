# Gateman Verification Report — BU-222 Bounded Hybrid Retrieval

**Feature:** tenant-filtered lexical/semantic/freshness ranking with bounded results
**Branch:** `codex/bu-222-hybrid-retrieval`
**Date:** 2026-07-10

## Evidence

- `bun test --exit packages/knowledge/retrieval.test.ts packages/knowledge/*.test.ts`: 4 passed, 0 failed.
- `git diff --check`: passed.

## Sections 1–8

1. **Input validation — PASS.** Workspace and result limits are required and bounded.
2. **External identifiers — PASS.** Candidates are filtered by explicit workspace before ranking.
3. **Side-channel/fraud surface — PASS.** Ranking is read-only and does not mutate canonical evidence.
4. **Configuration — PASS.** Weights are centralized and deterministic.
5. **Money/time/reversibility — N/A.** No state changes.
6. **Failure modes — PASS.** Invalid limits fail loudly; empty scopes return empty results.
7. **Observability — PASS with follow-up.** Evidence version and freshness are returned for trace/citation layers.
8. **AI output verification — PASS.** Tests cover tenant filtering, freshness ordering, and bounded retrieval alongside store/outbox tests.

## Verdict

**Risk:** MEDIUM until backed by indexed SQL/Typesense/pgvector query plans and benchmark SLO evidence.

**Safe to ship:** `YES_WITH_FOLLOWUPS` as a deterministic retrieval contract.
