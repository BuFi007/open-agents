# Gateman Verification Report — BU-220 Transactional Outbox Contract

**Feature:** deduplicated, bounded-claim outbox contract for KG BullMQ delivery
**Branch:** `codex/bu-220-transactional-outbox`
**Date:** 2026-07-10
**Verifier:** Codex (Gateman analysis)

## Evidence

- `bun test --exit packages/knowledge/outbox.test.ts packages/knowledge/store.test.ts`: 3 passed, 0 failed.
- `git diff --check`: passed.

## Sections 1–8

1. **Input validation — PASS.** Event identity, workspace, topic, and bounded claim size are required.
2. **External identifiers — PASS.** Events are workspace-scoped and idempotent by caller-provided event ID.
3. **Side-channel/fraud surface — PASS with follow-up.** Payload is opaque and no external publish occurs in this reference contract; production adapters must authorize topic routing.
4. **Configuration — PASS.** No queue/provider defaults are hidden in the outbox.
5. **Money/time/reversibility — PASS with follow-up.** Attempts are counted and published state is explicit; database transaction, lease expiry, and poison-event DLQ belong to the production adapter.
6. **Failure modes — PASS.** Duplicate appends converge and unknown publish IDs fail loudly.
7. **Observability — PASS with follow-up.** Attempts and status are queryable; native Eve lifecycle events should wrap claims/publishes.
8. **AI output verification — PASS.** Tests cover deduplication, bounded claims, retry count, and published suppression.

## Verdict

**Risk:** MEDIUM until implemented with a Postgres transaction and BullMQ publisher/outbox relay.

**Safe to ship:** `YES_WITH_FOLLOWUPS` as a reference contract.
