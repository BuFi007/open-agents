## Gateman Verification Report

**Feature:** BU-227 deterministic SourceArtifact envelope
**Branch / PR:** codex/bu-227-source-artifacts
**Date:** 2026-07-10
**Verifier:** Codex

### Score

| Category | Score | Note |
| --- | ---: | --- |
| Error handling | 8 | Missing workspace, connector, provider IDs, invalid hashes, MIME, sizes, and timestamps fail loud. |
| Logging | 6 | Pure identity contract emits no logs; callers must attach Eve trace and stage status events. |
| Type safety | 9 | Provider, redaction, and connector stage contracts are explicit TypeScript unions. |
| Testability | 9 | Manual dedupe, Gmail/Outlook native identity, filename non-identity, and invalid provider IDs are tested. |
| Performance | 9 | Identity construction is deterministic string work with no I/O or graph traversal. |
| Security | 8 | No credentials or raw document contents are included; storage reference and redaction class are metadata only. |
| AI verification | 8 | Edited files were re-read, imports/typecheck verified, and focused tests were observed passing. |

### Checks Passed

- [x] Manual upload identity is derived from workspace, connector, and content hash rather than random storage keys.
- [x] Gmail and Outlook identity includes provider account, native container/message ID, native artifact/attachment ID, and content hash.
- [x] Filenames are preserved only as labels and do not determine uniqueness.
- [x] Received time and observed time are distinct and validated.
- [x] Stage operation IDs are deterministic for BullMQ, KG, inbox, and trace lineage.
- [x] Focused tests passed: `bun test packages/connectors` -> 7 pass, 0 fail.
- [x] Typecheck passed: `bun run --cwd packages/connectors typecheck`.
- [x] Whitespace check passed: `git diff --check`.

### Checks Failed

- [ ] Live Manual/Gmail/Outlook workers are not rewired to emit this envelope in this Open Agents slice.
- [ ] No Redis/BullMQ plus ephemeral Postgres processor test is included here.

### Recommended Next Steps

1. Wire Magic Inbox manual upload, Gmail attachment, and Outlook attachment processors to persist this envelope.
2. Add a live replay fixture that proves duplicate delivery, rename, worker restart, and token refresh converge to one artifact.

### Risk Level

MEDIUM

### Sign-off

Safe to ship: YES_WITH_FOLLOWUPS
